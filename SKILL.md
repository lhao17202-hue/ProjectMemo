---
name: ProjectMemo
description: 项目结构记忆快照。当用户要求了解/浏览/阅读项目结构、说"记住项目"/"项目记忆"/"ProjectMemo"/"项目快照"/"生成项目文档"/"梳理项目结构"时触发。也适用于新会话开始、用户问"这个项目是做什么的"/"项目有哪些模块"等需要快速理解项目结构的场景。自动生成和维护 ProjectMemo.md，记录文件用途、模块关系、核心接口，避免 AI 每次重头扫描项目、重复消耗 Token。
---

# ProjectMemo — 项目记忆快照

## 概述

ProjectMemo 是一套轻量化的项目结构持久化记忆机制。核心思路很简单：

> **AI 浏览项目 → 把关键信息写入 ProjectMemo.md → 下次直接读这份记忆，不用重头扫描。**

没有脚本、没有复杂配置，纯靠 AI 按照本 Skill 的工作流来执行。

## 核心理念

### 为什么需要它

AI 编码最大的浪费：每次新会话都要重读项目结构、重新理解模块关系、重新梳理文件用途。同一份 `src/utils/helper.ts` 可能被不同的会话分别读了 10 遍，每次都消耗 Token 去理解同一件事。

ProjectMemo 解决的就是这个问题：**读一次，记下来，永久复用**。

### 设计原则

- **轻量优先**：不追求详尽文档，只记录"下次会话最需要的"关键信息
- **增量优先**：只更新变化的部分，不重写整个文档
- **可读优先**：人类和 AI 都容易阅读的 Markdown 格式
- **按需触发**：用户浏览项目时自动判断是否需要生成或更新

## ProjectMemo.md 格式规范

文件放在项目根目录。首次生成时创建，后续增量更新。

```markdown
# ProjectMemo — [项目名称]

>  最后更新: YYYY-MM-DD HH:MM |  跟踪文件: N 个 |  Git: abc1234

##  项目概览

- **项目类型**: [Web 应用 / CLI 工具 / 库 / ...]
- **技术栈**: [React + TypeScript / Python FastAPI / ...]
- **入口文件**: [路径]
- **构建/运行命令**: [关键命令]

##  目录结构

```
project/
├── src/           # 主源码
│   ├── utils/     # 通用工具函数
│   ├── components/# UI 组件
│   └── ...
├── tests/         # 测试
└── config/        # 配置文件
```

##  文件索引

> 每个文件条目约 3-5 行，只记关键信息

### src/utils/helper.ts
- **用途**: 通用工具函数集（日期格式化、字符串处理、数组操作）
- **主要导出**: `formatDate()`, `cn()`, `groupBy()`, `debounce()`
- **被引用**: `src/components/Table.tsx`, `src/pages/Dashboard.tsx`

### src/components/Button.tsx
- **用途**: 通用按钮组件，支持 variant/size/loading 属性
- **主要导出**: `Button` (default export)
- **依赖**: `src/utils/cn.ts`, `react`
- **被引用**: 全局 15+ 处

##  模块关系

- **utils/** → 被所有模块依赖，零外部依赖
- **components/** → 依赖 utils/，被 pages/ 引用
- **pages/** → 依赖 components/ + utils/，是顶层组装层
- **services/** → 依赖 utils/，负责 API 调用，被 pages/ 引用

##  变更记录

| 时间 | 类型 | 文件 | 说明 |
|------|------|------|------|
| 07-16 14:30 | 新增 | src/utils/fetch.ts | 统一 HTTP 请求封装 |
| 07-16 14:30 | 修改 | src/components/Modal.tsx | 新增 onClose 回调 |
| 07-15 10:00 | 删除 | src/old/legacy.ts | 已重构移除 |
```

## 工作流程

### 第一步：检查是否需要生成/更新

当用户让你浏览项目、阅读代码、或明确提到项目结构相关问题时，先检查：

1. **项目根目录是否存在 `ProjectMemo.md`？**
   - 不存在 → 进入「首次生成」流程
   - 存在 → 进入「增量更新」流程

2. **读取已有 memo，对比当前项目状态**：
   - 用 `git diff --name-only HEAD` 检查自上次记录以来有哪些已跟踪文件变动
   - 同时用 `git status --short` 和 `git ls-files --others --exclude-standard` 捕捉未跟踪的新文件，避免漏记刚创建但尚未 add 的核心文件
   - 用 `git log --oneline -5` 了解最近的提交
   - 没有 git 则对比 memo 中记录的文件列表和实际目录结构

### 第二步：首次生成

当 `ProjectMemo.md` 不存在时：

1. **快速扫描项目结构**：
   - 列出项目目录树（排除 `node_modules`、`.git`、`dist`、`build`、`__pycache__`、`.next` 等）
   - 读取 `package.json` / `pyproject.toml` / `go.mod` 等配置文件获取项目元信息

2. **识别核心文件**：
   - 入口文件（`index.ts`、`main.py`、`App.tsx` 等）
   - 配置文件（`tsconfig.json`、`.env.example` 等）
   - 工具/公共模块（`utils/`、`common/`、`shared/` 等）
   - 业务核心模块（`services/`、`pages/`、`controllers/` 等）

3. **按模板生成 ProjectMemo.md**：
   - 写入项目根目录
   - 文件索引只记录当前会话实际浏览过的文件（不是全部文件）
   - 未浏览的文件可以在目录结构中列出，但不在文件索引中详述
   - 在变更记录中标注"首次生成"

**重要**：首次生成不求覆盖所有文件。只记录已浏览的、能确认用途的文件。后续每次浏览新文件时，增量补充。

### 第三步：增量更新

当 `ProjectMemo.md` 已存在时：

1. **检测变更**：
   ```bash
   git diff --name-only HEAD
   git status --short
   git ls-files --others --exclude-standard
   ```
   或者对比 memo 中记录的文件和当前目录。

2. **判断是否需要更新**：
   - 仅有注释、空行、格式等微调 → **跳过**，不触发更新
   - 有实质性的文件新增/删除/移动/逻辑修改 → **触发增量更新**

3. **执行增量更新**：
   - **新增文件**：如果是当前会话浏览到的新文件 → 在文件索引中追加条目
   - **修改文件**：如果当前会话重新浏览了某文件 → 更新对应条目的用途和导出
   - **删除文件**：如果文件已不存在 → 从文件索引中移除条目，在变更记录中标注
   - **移动/重命名**：更新路径，保留原有描述

4. **更新元信息**：
   - 更新「最后更新」时间戳
   - 更新 Git commit hash
   - 更新跟踪文件计数

### 第四步：会话中使用

每次新会话开始，或用户问到项目相关问题时：

1. **首先检查 `ProjectMemo.md` 是否存在**
2. 如果存在 → **先读 memo，再决定是否需要额外扫描**
3. 大多数情况下，memo 里的信息足够回答用户问题，不需要重新扫描项目
4. 如果用户问到的文件不在 memo 中 → 读那个文件，回答用户问题，**顺手把新文件的信息追加到 memo**

## 变更检测策略

| 检测方式 | 适用场景 | 说明 |
|---------|---------|------|
| `git diff --name-only HEAD` | Git 项目（推荐） | 快速查看已跟踪文件的修改 |
| `git status --short` + `git ls-files --others --exclude-standard` | Git 项目补充检查 | 捕捉未跟踪的新文件，避免 ProjectMemo 漏记新模块 |
| 对比 memo 文件列表 vs 实际目录 | 非 Git 项目 | 稍慢但可行 |
| 对比文件修改时间 | 非 Git 项目备选 | 不够精确 |

### 什么触发更新 vs 什么不触发

**触发更新**：
- 文件新增/删除
- 函数签名变化（新增/移除参数、改名）
- 导出项变化（新增/移除导出）
- 模块职责变化（文件用途改变）
- 依赖关系变化

**不触发更新**（静默跳过）：
- 仅注释改动
- 仅格式化/缩进调整
- 仅变量名局部重命名（不影响对外接口）
- 连续 3 次以上同一文件的小改动（合并为一次更新）

## 最佳实践

1. **不求全量覆盖**：memo 是"增量生长"的——每次会话浏览了新文件就追加，慢慢积累。不需要一次性扫完整个项目。
2. **保持条目精简**：每个文件 2-4 行描述，不要写成详细文档。目的是快速定位，不是替代代码阅读。
3. **及时更新**：发现 memo 中的描述过时了，当场更新。过时的记忆比没有记忆更糟糕。
4. **利用 Git**：优先用 git 信息来判断变更，比手动对比高效得多。
5. **跨会话记忆**：ProjectMemo.md 是持久化文件，关机重启、换会话都不会丢失。这是它和"对话上下文记忆"最大的区别。
